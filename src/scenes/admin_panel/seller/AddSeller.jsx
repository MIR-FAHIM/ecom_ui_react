import React from 'react';
import { Box, Typography } from '@mui/material';

const AddSeller = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Add Seller</Typography>
    </Box>
  );
};

export default AddSeller;
