import React from "react";
import { Typography, Box } from "@mui/material";

const About = () => (
  <Box>
    <Typography variant="h4">About</Typography>
    <Typography variant="body1" sx={{ mt: 2 }}>Short about text...</Typography>
  </Box>
);

export default About;
